name: dashboard
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^4.0.0 # Use the latest version compatible with your project
  cupertino_icons: ^1.0.2 
  cloud_firestore: ^4.15.5
  firebase_auth: ^4.17.4
  firebase_core: ^2.25.4
  fluttertoast: ^8.2.4  
  http: ^0.13.3
  provider: ^6.0.1 # Use the latest version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0



flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/nethFM.png
    - assets/deranaFM.jpeg
    - assets/background1.jpeg
      
